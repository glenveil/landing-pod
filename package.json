{
  "name": "landing-pod",
  "version": "0.1.0",
  "description": "landing page",
  "scripts": {
    "build:css": "tailwindcss -i src/input.css -o docs/assets/tailwind.css --minify",
    "build:js": "terser \"src/*.js\" --compress --mangle --comments false --output docs/assets/app.js",
    "build": "npm run build:css && npm run build:js"
  },
  "author": "glenveil",
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "tailwindcss": "^4.3.0",
    "terser": "^5.48.0"
  }
}
